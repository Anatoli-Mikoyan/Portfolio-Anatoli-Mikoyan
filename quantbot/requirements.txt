# Cœur — indispensable (data, features, labeling, backtest, validation)
numpy>=1.24
pandas>=2.0
scipy>=1.10

# Apprentissage par renforcement — requis pour qbot.agents uniquement
torch>=2.0

# Méta-modèle supervisé et clustering de régime — requis pour qbot.ml et qbot.regime
scikit-learn>=1.3

# Modèle de Markov caché — requis uniquement pour qbot.regime.HMMDetector
# (les détecteurs par règles et par clustering fonctionnent sans)
hmmlearn>=0.3

# Configuration YAML (optionnel : le JSON fonctionne sans)
PyYAML>=6.0

# Tests
pytest>=7.4

# Connecteur MetaTrader 5 (Windows uniquement, optionnel)
# MetaTrader5>=5.0.45
